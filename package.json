{
  "name": "http-client",
  "version": "1.0.0",
  "description": "An easy and zero dependencies promise based http-client with Request and Response Converters.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "eQuid96",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "ci": "yarn test && yarn build",
    "watch": "yarn clean && tsc -b tsconfig.build.json -w --preserveWatchOutput",
    "build": "yarn clean && tsc -b tsconfig.build.json",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.5.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  }
}
